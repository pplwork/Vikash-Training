const initState={
    posts:[
        {id:3,name:'vikash'},
        {id:1,name:'jackmil'},
    ]
}

const rootReducer=(state=initState,action)=>{

    switch (key) {
        case value:
            
            break;
    
        default:
        return state;
    }
}

export default rootReducer